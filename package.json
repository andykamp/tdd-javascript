{
  "name": "tdd-javascript",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose --coverage",
    "e2e": "cypress open"

  },
  "devDependencies": {
    "@types/jest": "^28.1.2",
    "ts-jest": "^28.0.5",
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "jest": "^28.1.1"
  }
}
